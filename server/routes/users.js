const express = require('express');
const router = express.Router();
const User = require('../models/User');

// User portfolio routes would go here
// (Register, login, portfolio management, etc.)

module.exports = router;
